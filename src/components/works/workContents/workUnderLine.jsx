import styled from 'styled-components'
import * as color from "../../../css/color.js"

const WorkUnderLine = styled.span`
  background: linear-gradient(transparent 50%, ${color.accentYellow} 50%);
`;

export default WorkUnderLine;
